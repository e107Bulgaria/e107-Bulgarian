# e107 Bulgarian Language Pack

Професионален български езиков пакет за **e107 CMS 2.3.x**.

Текущата база на превода е **e107 v2.3.12**. Преводът е изготвен наново по официалните английски ресурси на e107 и не използва стария български превод като текстова база.

## Статус

Българският езиков пакет за **e107 v2.3.12 е завършен** по отношение на официалните core, admin, bundled plugin и bundled theme езикови ресурси.

Пакетът съдържа:

- **196 PHP езикови и административни help файла**;
- **107 core/admin PHP файла** — 35 основни, 44 административни и 28 файла в `admin/help/`;
- **85 PHP езикови файла** на стандартните bundled плъгини;
- **4 PHP езикови файла** на стандартните bundled теми;
- `e107_languages/Bulgarian/Bulgarian.xml` с `compatibility="2.3.12"`;
- преводи на основния e107 интерфейс и администрацията;
- преводи на стандартните bundled плъгини;
- езиковите ресурси на стандартните bundled теми;
- локализиран Voux news template, където upstream темата съдържа hardcoded видим текст;
- **21 legacy help файла** в `e107_docs/help/Bulgarian/`.

## Актуализация за e107 v2.3.12

Направено е директно сравнение на официалните upstream тагове **v2.3.11 → v2.3.12**.

Между двете версии има **551 upstream commits**. Променени са **22 съществуващи езикови ресурса**, добавен е нов frontend language файл за плъгина `signin`, а legacy `e107_languages/English/admin/help/cron.php` е премахнат, тъй като помощта за задачите по график вече е интегрирана в `admin/lan_cron.php`.

Основните промени в българския пакет са:

- нови съобщения за отказани действия при липсващ security token в core и администрацията;
- изцяло обновен превод на `admin/lan_cron.php`, включително новия раздел за настройка на `cron.php`, HTTP/CLI/shell варианти, Windows Task Scheduler, cPanel/DirectAdmin/Plesk и диагностиката на cron заявките;
- нови текстове за копиране във form handler, оценяване, регистрация, качване, потребителски вход и настройки на профила;
- нови текстове в Download, Forum, Newsletter и Private Messenger;
- нов български frontend language файл за bundled плъгина `signin`;
- премахнат е `admin/help/cron.php` в синхрон с upstream структурата.

Структурната промяна запазва общия брой от **196 PHP езикови/help файла**: core/admin файловете стават 107 заради премахнатия cron help файл, а plugin language файловете стават 85 заради новия `signin` language resource.

## Основен e107 превод

Преведени са всички актуални езикови ресурси от `e107_languages/English/`, включително:

- основни системни и публични текстове;
- администрация и настройки;
- всички 44 core admin language файла от `e107_languages/English/admin/`;
- всички 28 PHP help файла от `e107_languages/English/admin/help/`;
- вход, регистрация и възстановяване на парола;
- потребителски профили, потребителски класове и разширени полета;
- новини, коментари, оценяване и търсене;
- качване и обработка на файлове;
- BBCode/renderer помощни текстове;
- Library Manager, mail/form handlers;
- кеш, задачи по график и системни известия;
- файлов и медиен мениджър;
- управление на теми и плъгини;
- системни обновявания;
- инсталаторът на e107.

### Административна част

При финалния одит за v2.3.10 беше установено, че по-ранната версия на пакета съдържаше само 14 от 44-те core admin language файла. Липсваха 30 административни файла и цялата `admin/help/` директория.

Тези пропуски са коригирани и покритието е запазено при актуализацията до v2.3.12. Текущият пакет съдържа:

- **44/44** core admin language файла;
- **28/28** PHP файла в `admin/help/`;
- `admin/help/index.html`;
- запазени имена на константи, placeholders, HTML, BBCode и технически стойности от upstream.

## Стандартни плъгини

Езиковите ресурси на стандартните bundled плъгини от **e107 v2.3.12** са преведени или проверени като компоненти без собствен езиков файл.

Покрити са, наред с други:

- `news`, `page`, `download`, `faqs`, `featurebox`, `gallery`;
- `forum`, `pm`, `newsletter`, `chatbox_menu`, `comment_menu`, `poll`;
- `gsitemap`, `newsfeed`, `rss_menu`, `online`, `siteinfo`, `social`, `tagcloud`, `list_new`, `navigation`;
- `alt_auth`, `login_menu`, `signin`, `user`;
- `banner`, `blogcalendar_menu`, `hero`, `import`, `linkwords`, `newforumposts_main`, `tinymce4`, `_blank`.

Проверени са и `search_menu`, `contact` и `admin_menu`, които в официалния e107 v2.3.12 нямат собствени езикови файлове за превод.

## Стандартни теми

За bundled темите от e107 v2.3.12 се следва стандартната e107 структура за локализация:

- `_blank` — `languages/Bulgarian.php`;
- `bootstrap3` — `languages/Bulgarian_admin.php`;
- `bootstrap5` — `languages/Bulgarian.php` и `languages/Bulgarian_admin.php`;
- `voux` — няма собствена `languages` директория в upstream; добавен е локализираният `templates/news/news_template.php`, защото оригиналният шаблон съдържа hardcoded видими текстове.

Не се поддържат копия на оригиналните `theme.xml`, `theme.php`, `theme_config.php`, layouts и други theme runtime файлове само с цел превод.

## `e107_docs`

Добавен е професионален български превод на legacy help документацията в:

`e107_docs/help/Bulgarian/`

Преведени са **21 help файла** с общо **59 въпроса и 59 отговора**:

- `Administrators`
- `BBCode`
- `Banlist`
- `Banners`
- `Cache`
- `Chatbox`
- `Classes`
- `Downloads`
- `Emoticons`
- `Errors`
- `Forums`
- `Front_Page`
- `Help!`
- `Links`
- `Maintainance`
- `Menus`
- `News`
- `Preferences`
- `Uploads`
- `Users`
- `Welcome_Message`

### Източник на документацията

`e107_docs` не присъства в официалното core дърво на **e107 v2.3.12**. Последната използвана официална English база за тези help файлове е `e107inc/e107` **v2.1.9**, където `e107_docs/help/English/` все още е част от проекта.

Поради това тези файлове трябва да се разглеждат като **legacy помощна документация**. Някои описани функции, URL пътища, стари браузъри, права `chmod 777`, `resetcore.php` и други процедури може да не са приложими към съвременна e107 инсталация.

Запазени са форматът `Q>` / `A>`, имената на help файловете и техническите маркери. Терминологията е уеднаквена с текущия български пакет, включително **„Потребителски клас“** за e107 `User class`.

GNU GPL лицензният текст не се превежда свободно като част от този етап, за да не се представя неофициален превод като юридически еквивалент на оригиналния лиценз.

## Финална проверка

Основният езиков пакет е проверен спрямо официалните English ресурси на **e107 v2.3.12**.

Покрити са:

- **35** основни PHP езикови файла;
- **44/44** core admin language файла;
- **28/28** PHP admin help файла;
- **85** езикови файла на стандартните bundled плъгини;
- **4** езикови файла на стандартните bundled теми;
- **21** legacy help документа от `e107_docs`.

Общо в актуалната runtime езикова част има **196 PHP файла**.

При разработката и финалния одит са извършени:

- сравнение на официалните тагове `v2.3.11` и `v2.3.12`;
- сравнение на имената и структурата на English/Bulgarian езиковите ресурси;
- сравнение на активните `define()` константи с English файловете;
- проверка за липсващи, допълнителни и дублирани константи;
- проверка на placeholders, shortcodes, HTML и BBCode маркери;
- проверка на административните help файлове с PHP логика без промяна на функционалното поведение;
- уеднаквяване на терминологията;
- проверка на `Q>` / `A>` структурата на `e107_docs`;
- сравнение на Git blob SHA стойности след публикуваните промени.

### Обхват на „завършен превод“

Статусът „завършен“ означава, че са покрити официалните **езикови ресурси** на e107 v2.3.12 и описаните в този пакет допълнителни локализации.

Текст, който е hardcoded директно в core/plugin/theme PHP, HTML, JavaScript или XML код и за който upstream не предоставя language resource, не може да бъде заменен само чрез стандартен language pack без override на оригиналния runtime файл. Такива случаи трябва да се разглеждат отделно, ако бъдат открити в реалния интерфейс.

## Поддръжка

При следващи версии на e107 трябва да се сравняват новите и променените English езикови ресурси с текущия български пакет.

За `e107_docs` трябва да се има предвид, че документацията е legacy и е премахната от съвременното e107 core дърво. Ако upstream възстанови или замени help документацията, преводът трябва да бъде актуализиран по новия официален източник.
